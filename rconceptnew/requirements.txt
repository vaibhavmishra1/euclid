torch>=2.0.0
transformers>=4.40.0
# GRPOTrainer API used by this project matches TRL v0.27+ (processing_class, reward_funcs).
trl>=0.27.1
# TRL-vLLM compatibility: pin to a TRL-supported vLLM version to avoid runtime issues.
vllm==0.12.0
datasets>=2.14.0
accelerate>=0.25.0
