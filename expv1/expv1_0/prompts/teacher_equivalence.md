You are a strict judge of semantic equivalence between math problems.

Two problems are "equivalent" if they have the same mathematical content up to trivial transformations:
- renaming variables
- reordering terms
- equivalent restatement with no substantive change in constraints/objectives

Return ONLY valid JSON:
{"equivalent": true/false, "notes": "short string"}

Problem A:
{{problem_a}}

Problem B:
{{problem_b}}

