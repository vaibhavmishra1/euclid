## Minimal dependencies to run evaluation/* only (no training).
##
## This avoids optional/compile-heavy training deps like flash-attn/xformers.
##
## GPU requirements:
## - NVIDIA GPU + recent driver
## - torch should already be CUDA-enabled (e.g. 2.7.0+cu126)
##
## Core runtime
torch==2.7.0
transformers==4.52.4
tokenizers==0.21.1
accelerate==1.7.0

## Inference
vllm==0.9.1

## Data + scoring
datasets==3.6.0
pandas==2.3.0
tqdm==4.67.1
math-verify==0.7.0
latex2sympy2_extended==1.10.1
sympy==1.14.0

## Common utilities (usually pulled transitively, but pinned for stability)
numpy==2.2.6
requests==2.32.4
